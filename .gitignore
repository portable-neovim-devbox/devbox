# avoid other git repositories in subdirectories
*.git/

# dotfiles are managed externally; keep only structure
dotfiles/**
!dotfiles/**/
!dotfiles/**/.gitkeep

# ignore all files in projects/
*projects/*

# ignore all files in ssh/ except sshd_config
ssh/*
!ssh/sshd_config

*README.html
